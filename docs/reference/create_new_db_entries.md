# Create new database entries for cultivars that aren't in the controlled vocab (deprecated)

Need to standardize crop type before the curation process This function
is based on create_newdbnames() and create_newdbnames_db()

## Usage

``` r
create_new_db_entries(
  auxiliary_files,
  filename,
  matches_temp_list,
  data_intid,
  match_type
)
```
