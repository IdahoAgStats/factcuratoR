# Read in database control variables

Note: See read_delim if need to escape backslashes

## Usage

``` r
readin_db_init(db_folder)
```

## Arguments

- db_folder:

  A string path to the database controlled vocabulary folder
