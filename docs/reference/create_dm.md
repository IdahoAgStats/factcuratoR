# Create a data model object from the main codebook

This function creates a data model from the codebook to show the
relationship between the controlled variables (e.g. where the controlled
vocabularies are defined)

## Usage

``` r
create_dm(codebook_main_path)
```

## Arguments

- codebook_main_path:

  A string that is a path to the main codebook
