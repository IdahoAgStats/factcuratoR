# Get cultivar rename list to match. The cv_rename.csv is located in the controlled vocabularies.

Get cultivar rename list to match. The cv_rename.csv is located in the
controlled vocabularies.

## Usage

``` r
get_cultivar_rename(rename_df_path)
```

## Arguments

- rename_df_path:

  The path of the file that contains the variety misspellings
