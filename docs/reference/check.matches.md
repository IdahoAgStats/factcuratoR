# Check that matches have one database match

Check that matches have one database match

## Usage

``` r
check.matches(match_df)
```

## Arguments

- match_df:

  A data.frame with variety names and database matches (including
  volumns var_id and db_id)
